# frozen_string_literal: true

RSpec.describe Rack::SecFetchSite do
  it "has a version number" do
    expect(Rack::SecFetchSite::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
